# Terraform and provider version constraints (see .claude/skills/aws-infra.md)
terraform {
  required_version = ">= 1.7.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.50"
    }
  }
}
