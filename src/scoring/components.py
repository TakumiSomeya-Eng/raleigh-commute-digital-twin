"""FR-10.1 – FR-10.6 — Six scoring component penalty functions.

Each returns a scalar in [0, 1]. Weights and thresholds from config/scoring.yaml.

Implemented in tasks T4.5 (jerk, harsh brake, lat accel) and T4.6 (speed, deviation, lane change).
"""
